IF  EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'{databaseOwner}[{objectQualifier}GFM_Custom_Items]') AND type in (N'U'))
DROP TABLE {databaseOwner}[{objectQualifier}GFM_Custom_Items]
GO
